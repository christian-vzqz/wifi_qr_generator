# Configuración de Netlify para aplicación React SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build]
  command = "pnpm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"